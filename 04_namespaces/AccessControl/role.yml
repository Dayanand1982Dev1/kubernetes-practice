apiVersion:  rbac.authorization.k8s.io/v1
kind: Role     # Name space level access control
metadata:
  name: pod-and-podlog-reader
  namespace: dev-team
rules:
- apiGroups: 
    - ""
  resources: 
    - pods
    - pods/log
  verbs: ["get", "list"]